FileManager : 

dist path will be identified later. 

contruct : sourceFile ;

identify the columns names .
identify each columns types.
For each names , we gonna append the field name and the field type 

Append those to the file 




method -> returns names : ARRAY ; 
method -> returns types : ARRAY ;

Loop through each Object : 

method -> Identify the file name and return it 

method (distFile) -> loop and change the template name and type then append it in the file after getting its name from the previous method 


Main method : 

super(sourceFile) 

excute() : get the names -> get the types -> identify the distFile -> append to the file 
 



